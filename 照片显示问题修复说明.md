# 照片换网络就裂图 —— 根因与修复

## 一、根因（已实测）

在办公网络下直连 Supabase 实测：

```
Trying 104.18.38.10:443...
Recv failure: Connection was reset      ← TCP 连接被重置，TLS 握手都没完成
```

`lrjiiacbzpcfpstxakuu.supabase.co`（Cloudflare IP）在**不少国内网络下会被重置**，不只是图片，数据同步也走不通。
所以现象是：上传时那个 WiFi 一切正常，换个网络后页面还能打开（读的是本机缓存），但图片全部裂开。

**旧版本的加载逻辑是「直接把 img.src 指向 supabase」，域名不通就只能裂图**，回退链还会因为「既不成功也不报错」一直挂着不触发。

## 二、这次改了什么（index.html）

1. **缩略图随数据同步（核心）**
   每张照片额外生成一张约 10KB 的小图，存在 `state.photoThumbs` 里，跟着家庭数据一起同步。
   **只要设备同步到过数据，就一定能显示出照片**（哪怕此后永远连不上 supabase），网络好时再自动换成高清原图。
   - 新上传的照片：立即生成缩略图。
   - 历史照片：打开应用 5 秒后**自动静默补建**（本机有副本或能加载出原图时），也可在「⚙️ → 🤖 API 设置 → 🧩 补建缩略图」手动触发。
   - 上限 150 张，超出自动淘汰最早的；本地存储真写满时会自动清空缩略图腾地方，不会导致保存失败。

2. **多通道 + 超时回退**
   代理 → 直连 → 本机 IndexedDB 副本 → 占位图（可点击重试）。每通道 6 秒超时，
   解决「域名不通时请求挂着不报错、回退永远不触发」的问题；成功的通道会被记住，下次优先走。

3. **上传也能走代理**：直传 Supabase 失败时自动改走图片代理上传（代理端带 anon key 回源）。

4. **新增两个诊断按钮**（⚙️ → 🤖 API 设置）：
   - `🔍 图片自检`：逐通道测试并直接告诉你哪个通、耗时多少、缩略图有没有。
   - `🧩 补建缩略图`：给老照片补缩略图，补完自动同步给其它设备。

5. 代理地址支持填多个（逗号分隔），且**会随家庭数据同步**，填一次所有设备生效。

## 三、你需要做的三步

### 1. 推送到 GitHub（命令行需要凭据，请自己执行）

```bash
cd C:/Users/11637/WorkBuddy/20260504223628/jijilili-app
git push origin main
```

### 2. 在「能连上 Supabase 的那个网络」下，让每台设备各打开一次

这是关键一步：缩略图要借一次成功的同步传到手机。

- 电脑打开网页版 → 停 10 秒（首页有照片的话，日历/食谱页面加载出照片即可，程序在后台自动补缩缩略图）
- 手机打开网页版 → 同样停 10 秒，等同步完成
- 不放心就点「🧩 补建缩略图」，它会显示 `补建完成：x/y 张已生成缩略图，正在同步给其它设备…`

之后你换到任何网络（包括连不上 supabase 的网络），照片都能显示（缩略图已在本机）。

### 3. 手机强制刷新一次

GitHub Pages 有缓存，手机请 `Ctrl+Shift+R`（安卓 Chrome 长按刷新 → 强制刷新），
或从桌面图标打开后下拉刷新。Service Worker 已升级到 v5，会自动清掉旧缓存。

## 四、可选：部署国内图片代理（想让高清原图也更稳时再做）

`aliyun-fc-proxy/photo-index.js` 是阿里云函数计算版图片中转（杭州，国内直连可达），
部署后把函数 URL 填进「🖼️ 图片代理地址」即可，全家设备自动生效。
`cloudflare/photo-proxy.js` 是 Cloudflare Worker 版，二选一即可（都已支持读取 + 上传转发）。

> 提醒：阿里云/Cloudflare 的出口同样要访问 supabase.co，若被重置，代理也取不到原图。
> 所以**缩略图方案才是保底**，代理只是让高清图多一条路。若长期受此困扰，
> 建议把照片与数据整体迁到国内可达的存储（如七牛云 / 腾讯云 COS / 阿里云 OSS），我可以帮你改。
