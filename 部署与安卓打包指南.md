# 吉吉利利 · 部署到 GitHub 并打包安卓 APP 指南

> 本目录已经为你整理成"可直接部署"的结构。下面的命令你照抄执行即可。
> 说明：我（AI）没有你的 GitHub 登录凭据，**"推送到 GitHub"这一步需要你本人操作**。我已把文件、清单、图标、离线缓存都准备好，并给出复制即用的命令。

---

## 一、项目里现在有什么

```
jijilili-app/
├── index.html                 # 主程序（已接入 PWA 清单 + 离线缓存）
├── manifest.webmanifest       # PWA 应用清单（名称/图标/全屏样式）
├── sw.js                      # Service Worker，实现离线打开
├── icon-192.png               # 图标 192
├── icon-512.png               # 图标 512
├── icon-512-maskable.png      # 安卓自适应图标（满铺）
├── .nojekyll                  # 让 GitHub Pages 不处理下划线文件
└── gen_icons.py               # 图标生成脚本（备用，可改色/改字重生成）
```

这些都已验证可在本地 `http://localhost:8123` 正常打开。

---

## 二、部署到 GitHub（你来执行）

### 第 1 步：在 GitHub 新建仓库
1. 打开 https://github.com → 右上角 **＋ → New repository**
2. 仓库名填 `jijilili`（随意），**公开 Public**，**不要**勾选 "Add a README"
3. 点 **Create repository**，进入后复制仓库地址，形如：
   - HTTPS：`https://github.com/你的用户名/jijilili.git`
   - SSH：`git@github.com:你的用户名/jijilili.git`

### 第 2 步：在电脑终端推送（复制粘贴）
打开终端（Windows 可用 Git Bash / PowerShell，进入本目录）：

```bash
cd C:/Users/11637/WorkBuddy/20260504223628/jijilili-app

git init
git add -A
git commit -m "初次提交：吉吉利利家庭管理工作台"

git branch -M main
git remote add origin https://github.com/你的用户名/jijilili.git
git push -u origin main
```

> 关于登录：GitHub 自 2021 年起不再支持"账号+密码"推送，请用以下任一方式：
> - **个人访问令牌(PAT)**：GitHub → Settings → Developer settings → Personal access tokens → 生成（勾 `repo` 权限）。推送时"用户名"填你的 GitHub 用户名，"密码"填这个 token。
> - **SSH 密钥**：更省事，配一次永久免密。把 `origin` 换成 `git@github.com:...` 格式即可。

推送成功后，代码就上 GitHub 了。

### 第 3 步：开启 GitHub Pages（免费托管 + HTTPS）
1. 进仓库 → **Settings → Pages**
2. Source 选 **Deploy from a branch**，Branch 选 **main**，目录选 **/ (root)**
3. 点 **Save**。等 1～2 分钟
4. 访问：`https://你的用户名.github.io/jijilili/`

这个网址就是你的线上应用，也是后面打包安卓 APP 要用的地址。

---

## 三、做成安卓手机桌面的 APP（三种方法）

### 方法 1（最快，免打包，建议先试）：浏览器"添加到主屏幕"
1. 手机上用 **Chrome / Edge** 打开 `https://你的用户名.github.io/jijilili/`
2. 点右上角 **⋮ → 添加到主屏幕 / 安装应用**
3. 桌面出现「吉吉利利」图标，点开即**全屏独立运行**，和原生 APP 一样

- 优点：零安装、立刻能用，数据存在该浏览器里。
- 缺点：依赖手机上的 Chrome/Edge 浏览器。

### 方法 2（真正的 APK 安装包，可发给别人/上架）：PWABuilder
微软出品的免费工具，把 PWA 一键打包成签名 APK。

1. 先完成上面的"部署到 GitHub Pages"（必须有 https 公网地址 + 清单 + 图标，本目录已备齐）。
2. 电脑浏览器打开 https://www.pwabuilder.com
3. 输入你的网址 `https://你的用户名.github.io/jijilili/` → 点 **Start**
4. 检测通过（manifest / 图标 / service worker 都 ✅）后，点 **Build → Android**
5. 下载 **Android APK (TWA)**；页面提供"已签名 APK（调试用）"可**直接安装**
6. 把 `.apk` 传到安卓手机 → 允许"未知来源"安装即可

> 想上架 Google Play：在 PWABuilder 里自行生成签名密钥（页面有图文指引），再提交 AAB。

### 方法 3（完全离线、不联网也能用）：WebView 打包
适合不想依赖服务器的情况——把 `index.html` 直接打进 APK 的 WebView，数据存在 App 内部。

- 省事办法：在线工具（如 AppsGeyser 免费、WebViewGold 付费）上传 HTML 即生成 APK。
- 进阶办法：Android Studio 新建 Empty Activity，用 `WebView` 加载 `assets/index.html`，开启 JavaScript 与 DOM Storage。需要我给你出一套 Capacitor/Android Studio 的离线打包模板，可以再说。

- 优点：离线可用、数据完全在 App 内。
- 缺点：需要打包工具或 Android Studio 环境。

---

## 四、重要提醒

- **数据在本地**：所有内容存在手机浏览器 / App 的 localStorage，**不联网、不上传任何服务器**。
- **换设备**：App 内「关于 & 数据 → 导出数据」生成 json，再到新设备「导入数据」即可搬家。
- **AI 推荐功能**：需自备一个 OpenAI 兼容的 API Key（在 App 内「⚙️ API 设置」填写），密钥只存本机。
- **公开仓库无忧**：GitHub Pages 是公开网址，但别人只能打开"空壳"界面，你的数据只在你本机，不存在隐私泄露。

---

## 五、我可以继续帮你做的

- 配置 **GitHub Actions 自动部署**：以后你只要 `git push`，网页自动更新。
- 用 **Capacitor** 生成真正的离线 APK（需要本机有 Android Studio / JDK）。
- 换图标文字/配色：改 `gen_icons.py` 里的 `BG` 和 `TEXT = "jili"`，重跑 `python gen_icons.py` 即可。
- 接入 **多用户数据同步后端**（见第六节）。

---

## 六、多用户数据同步说明

当前 App 的所有数据都存在本机浏览器的 `localStorage` 里，**天然不支持多设备/多人同步**。要实现"多个用户看到同一份同步数据"，必须接入一个云数据库/后端。

### 可选方案

| 方案 | 是否推荐 | 说明 |
|------|----------|------|
| **Supabase** | ⭐ 推荐 | 免费 PostgreSQL + 实时订阅 + 登录体系；我可以帮你建表、改代码。 |
| **Firebase** | 可用 | Google Realtime Database / Firestore；国内访问偶尔不稳定。 |
| **用 GitHub 文件当共享存储** | 不推荐 | 把数据存在一个 JSON 文件里，每次打开拉取；不适合多人同时编辑，容易冲突。 |
| **Cloudflare D1 / Workers** | 进阶 | 免费、轻量，但需要写 Worker API，复杂度稍高。 |

### 你需要先决定的两个问题

1. **访问模式**：
   - **共享一份家庭数据**：所有人输入同一个"家庭码"就看到同一套冰箱/食谱/待办（最简单）。
   - **每人独立账号，再组成家庭**：需要注册/登录，数据按账号隔离，再选择共享给哪些家庭成员。

2. **是否允许离线使用**：
   - 可以保留离线编辑，联网后自动同步（需要做冲突处理，稍复杂）。
   - 也可以做成纯在线版（简单，但没网不能用）。

### 我需要你提供什么（以 Supabase 为例）

- 一个免费 Supabase 账号 & 项目。
- 项目的 `Project URL` 和 `anon public API Key`（在 Supabase Dashboard → Project Settings → API 里）。
- 你希望的访问模式（共享家庭码 / 账号登录）。

---

## 七、让我帮你部署 GitHub，你需要提供什么

我（AI）当前没有你的 GitHub 登录状态，所以"代你推送"需要下面任一方式：

### 方式 A（推荐、最安全）：在 WorkBuddy 里连接 GitHub

如果 WorkBuddy 的 GitHub 连接器支持代码仓库操作，你只需在 WorkBuddy 设置里授权/连接 GitHub（状态目前显示 disconnected）。连接成功后告诉我仓库名，我可以直接推送，**你不需要把 token 发给我**。

### 方式 B：给我 Personal Access Token（PAT）

如果方式 A 走不通，可以给我 token。为了安全，请使用 **Fine-grained personal access token**：

1. 打开 https://github.com/settings/tokens?type=beta
2. 点 **Generate token**
3. 命名如 `deploy-jijilili`，有效期选 7 天或 30 天（用完即删）
4. **Repository access** 选 `Only select repositories`，勾选你新建的 `jijilili` 仓库
5. **Permissions → Repository permissions** 里把 **Contents** 设为 `Read and write`
6. 点 **Generate token**，复制 token（只显示一次）

然后发给我：
- 你的 **GitHub 用户名**
- 仓库名（例如 `jijilili`）
- 上面生成的 **token**

我会用 `git remote add origin https://<token>@github.com/<用户名>/<仓库>.git` 的方式帮你推送。推送完成后，你可以立刻在 GitHub 上删除这个 token。

### 方式 C：你自己执行最后一条推送命令

你完全可以在本地终端运行（不用把 token 给我）：

```bash
cd C:/Users/11637/WorkBuddy/20260504223628/jijilili-app
git remote add origin https://<你的PAT>@github.com/<用户名>/jijilili.git
git push -u origin main
```

然后回到第二节继续开启 GitHub Pages 即可。
